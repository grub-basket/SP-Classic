# 0.52.0 — Per-parent sort

## Per-parent sort dropdown
- Each parent in the tree gets its own sort mode (date created / modified /
  manual / alphabetical / …).
- Dragging a child of an auto-sorted parent reverts that parent to manual
  mode, so a manual reorder always wins without nuking the rest of the tree.
